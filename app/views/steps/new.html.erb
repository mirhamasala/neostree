<h2 class="heading heading--form"></h2>

<%= form_with model: @step, url: [@recipe, @step], class: "form" do |f| %>
  <div class="form__main">
    <div>
      <%= f.label :description %>
      <%= f.text_area :description %>
    </div>
  </div>

  <%= f.submit %>
<% end %>
